import { Ubuntu, Poppins } from "next/font/google";

export const ubuntu = Ubuntu({
  weight: ["400", "700"],
  subsets: ["latin"],
});

export const poppins = Poppins({
  weight: ["400", "700"],
  subsets: ["latin"],
});
